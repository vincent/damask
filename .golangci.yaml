version: "2"

linters:
  settings:
    errcheck:
      exclude-functions:
        - strings.Builder.WriteString
  exclusions:
    rules:
      - linters:
          - errcheck
        source: "^\\s*defer\\s+"

run:
  timeout: 5m
